# Revalidate JavaScript and CSS, including SPA fallbacks for deleted asset
# URLs. Never mark /assets/* immutable: a missing asset is served as index.html.
/assets/*.js
  Cache-Control: public, max-age=0, must-revalidate

/assets/*.css
  Cache-Control: public, max-age=0, must-revalidate
