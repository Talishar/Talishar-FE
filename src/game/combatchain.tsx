import React from 'react';

export function CombatChain() {
  const height = (window.innerHeight / 8) * 1.5;
  return (
    <div className="CombatChain" style={{ height: height }}>
      COMBAT CHANE PEW PEW PEW
    </div>
  );
}
